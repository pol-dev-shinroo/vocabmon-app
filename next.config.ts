/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  // Note: If you are using next/image, you might need to set unoptimized: true
  // images: { unoptimized: true }
};

export default nextConfig;
