/** @type {import('next').NextConfig} */
const nextConfig = {
  // We removed 'output: "export"' so Next.js knows this is a full-stack app!
  // Note: If you are using next/image, you might need to set unoptimized: true
  // images: { unoptimized: true }
};

export default nextConfig;
